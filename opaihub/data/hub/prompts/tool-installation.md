Tool request:
{{tool}}
Existing registry matches:
{{matches}}

If duplicate exists, recommend reuse. Otherwise produce registry metadata and a project-local install plan.
Reject paid/subscription tools unless optional and disabled by default.
