# OPai E2E QA — PASS 2 (post-fix re-verification + new-area coverage) — 2026-07-18

**Method:** Reinstalled OPai from the fixed branch (`fix/qa-e2e-issue219`, HEAD `e1e9a00`) via `install.ps1` so the desktop GUI runs the fixed code (verified: pip editable install re-pointed from the stale `~/.opai/source` @ `main` to the repo). Then drove the real PySide6 desktop GUI via remote control, workspace = the OPai repo (842 files indexed). Re-ran the same natural prompt **"Solve GitHub issue #219 in this repo for me."** on Claude Haiku 4.5 (Safe Auto + Full Auto) and the free Gemini/Groq models, plus a focused edit-capability probe, then exercised areas untouched in pass 1.

**Headline verdict:** **The core "OPai can't solve a git issue" failure is substantially FIXED.** OPai now (1) routes "solve/fix" to an edit-capable mode instead of read-only Explain, (2) **actually makes code changes end-to-end** (verified on disk), (3) is **honest** — no fabricated "successfully solved", no unauthorized destructive `gh issue close`, blocked actions render as red ✗ with real reasons, and (4) the three mode controls now agree. Remaining friction is smaller and mostly about the in-GUI *edit-approval affordance*, a command-length limit, long/expensive runs, and free models returning nothing in this environment.

Pass-1 baseline: `docs/QA_E2E_ISSUE219_2026-07-17.md` (23 findings F2–F24). Fix map: `docs/QA_E2E_ISSUE219_RESOLUTION.md`.

---

## ✅ FIXED (verified by driving the reinstalled app)

| # | Pass-1 bug | Pass-2 evidence |
| --- | --- | --- |
| **F5 / F10 / F18** | "Solve/fix" classified as read-only **Explain**; task focus stuck on Explain | "Solve issue #219" now runs as Task focus **Build** / Agent mode **Implement**; the agent actively **tried to edit/write code** ("My task is to implement the fixes…"), never the old read-only patch-plan. |
| **F16 / F4** | Composer run-mode desync after workspace switch (showed Full Auto, engine was Safe Auto) | Composer + header + inspector Run mode **all agree** after workspace switch and fresh chat. Safe Auto→Full Auto correctly fires the **"Pin Full Auto?"** acknowledgement; after pinning, all three read Full Auto. |
| **F20 / F21** | Three mode concepts disagree; "Agent mode" stale | Single source of truth holds: Task focus Build → Agent mode Implement, consistently; new **"Agent mode (next run)"** field refreshes live. |
| **F8 / F11 / F14** | Green "Completed" / "✓ Ran" for no-op / blocked runs | Every blocked/failed action renders **red ✗ with an honest reason** ("requires approval", "you haven't granted it yet", "command too long"). Task cards say **"Completed with no changes — there is no diff to review"** instead of faking success. |
| **F22 / F24** | Fabricated completion; **actually closed real #219** with a false comment | The agent honestly concluded **"I've completed a thorough analysis… I've encountered a permission issue preventing me from modifying the codebase"** — NO "successfully solved", NO `gh issue close`. It even read the F23 destructive-gate code (`safety_gates.py`, `aci.py`) to confirm the guard. #219 is currently OPEN. |
| **F17** | Safe Auto hard-BLOCKs "Run any command" with no escalation | Safe Auto now shows **"Run any command = ASK"** (perms `3 allowed · 3 ask · 2 blocked`, was `2 ask · 3 blocked`); approval-needed commands fail honestly. New **compound-command approval parser** ("the following part requires approval: `gh issue view 219`"). |
| **F19** | `gh issue view` returned empty body, marked "✓ Ran", fed nothing to model | Plain `gh issue view` now renders **✗ failed** (not fake green); the agent obtains issue context from the reliable path. Empty/blocked output is surfaced, not swallowed. |
| **F13 / F15 (loop half)** | Redundant identical commands; dead-end loop | No identical-command thrash; the agent **adapts** around gated commands (dropped `cd &&`, retried a bare `find` which then succeeded). Not the pass-1 "please approve" dead-end. |
| **F6 / F7 (honesty half)** | Free Gemini **narrated** a fake `Tool Call:` as prose + reported "Completed" | Free path no longer fabricates. It honestly reports **"Stopped without finishing / The free-tier API did not return an answer"** and "Completed with no changes". (Real tool execution unverified — see below.) |
| **F3** | No branded exe (OS dialogs show `pythonw.exe`) | Documented in `docs/BRANDED_EXECUTABLE.md` (packaging follow-up). Still `pythonw.exe` in the OS control dialog — as designed for the pip path. |

### 🏆 The single biggest proof — OPai now actually makes changes
Focused probe: **"Create qa_probe.txt containing 'probe ok'."**
- **Safe Auto:** write gated → ✗ "you haven't granted it yet" → honest "OPai finished with no changes". (See F26.)
- **Full Auto (pinned):** ✓ **Wrote file: qa_probe.txt**; inspector Diff review `0 approved · 1 pending · 0 rejected`. **Ground truth on disk: file existed, 8 bytes, content exactly `probe ok`.** In pass 1 OPai could never make *any* change; now it can, end-to-end.

---

## ❌ STILL BROKEN / ⚠️ NEW ISSUES (pass 2)

| # | Sev | Finding |
| --- | --- | --- |
| **F26** | MEDIUM | **No in-GUI "approve this edit" affordance in Safe Auto.** A gated Edit/Create fails honestly and the agent asks **"…Should I proceed?"** in prose, then the run ends — there is no clickable Grant button. Replying "yes proceed" starts a fresh turn (the old loop). Workaround: switch to Full Auto (edits ALLOW) — which works. Reduced severity vs pass-1 F9 (honest + a working alternative path), but the promised inline edit-approval card isn't surfacing for the Claude-bridge edit/write path. NB: an inline actionable card *does* exist for the free-tier privacy gate, so the pattern is available to reuse here. |
| **F25** | MEDIUM (new) | **Hard 965-byte command limit.** A legitimate multi-line PowerShell edit failed: *"Command too long for parsing (1391 bytes). Maximum supported length is 965 bytes."* Blocks real work involving longer commands/heredocs/scripts. |
| **F27** | MEDIUM (new, efficiency) | **Expensive, non-converging run.** One Safe-Auto Haiku "solve #219" run = **~6 min, 61+ steps, $0.62, 0 diffs** (I stopped it). It over-explores instead of concluding "these fixes already exist." Budget went $0.27→$0.89 in one run. Not the pass-1 dead-end (it's productive + honest + bounded), but a single $0.62 no-diff run is a cost concern. |
| **F29** | LOW (new) | **Free-tier confirmation names the wrong provider.** With **Groq** selected, the privacy card + button still say *"send your task to **Gemini's** public API"* / **"Send to Gemini."** Hardcoded provider name → misleading disclosure. |
| **F28** | LOW (new) | **"! Used ToolSearch: select:TaskCreate,TaskUpdate — no output returned"** glitch in the agent's tool space. Cosmetic. |
| **F6/F7** | — (unverified) | **Free models are non-functional in this environment.** Both Gemini and Groq free returned **no answer** after the privacy gate ("did not return an answer", $0.00). Honest, but unusable — likely missing key/quota. Consider surfacing "not configured" explicitly instead of a generic no-answer, and verify the free-model tool loop where the API actually responds. |
| **obs** | LOW | Inspector **"Workflow: Completed"** shown after a *stopped*, 0-diff run — slightly generous label (Diff review honestly shows 0/0/0, so not a fabrication). Consider "Stopped/Incomplete". |
| **obs** | LOW | **"New app" scaffold ignores the description** — asked for "a tiny cli", got a fixed **web** template (index.html/styles.css/app.js). Fine as a free starting skeleton, but the label implies tailoring. |

---

## 🆕 Areas tested in pass 2 (not covered in pass 1) — all functional

- **Edit/Write capability** — works end-to-end in Full Auto (verified on disk). *[the headline fix]*
- **Prompt Library** — curated templates with categories (CODING/DEBUGGING/TESTING), search + filter. "Use prompt" loads into the composer **without auto-sending** and sets the matching Task focus (e.g. "Explain this repo" → Explain). Good.
- **Insights ▸ Money Saved** — real routed data ($1.01 saved, OPAI ON, Protected clients 6/6, Paid calls avoided 14, Context reduced 8,685) with honest disclaimers ("Not an official SWE-bench… result"). `CAPTURE HEALTH` shows an empty "—" (minor).
- **Insights ▸ Cost Firewall** — Policy `solo-balanced`, Panic off, **Spent today $0.93** (consistent with header), Cloud gate "confirm", local-first route chain, "No blocked paid/cloud actions recorded", Enable-panic button.
- **New app (OPai Build)** — deterministic free scaffolding: created 5 files (~1,092 tokens "never spent"), verified on disk.
- **Settings ▸ Models & Routing** — Defaults (model / run mode "Ask" with the "Full Auto is composer-pinned" note / task focus / output format) + local-first route order + `opai why` explainability.
- **Settings ▸ Permissions & Safety / Providers & Connections** — per-mode permission matrix; Claude = Verified, Codex/Copilot = Detected.
- **NEW — Free-tier API privacy gate** — before any free-model call: "your task and repository file contents will leave this device… Continue?" with actionable buttons. Good privacy guard.
- **NEW — Session persistence** — "Resume your previous work? (10 saved messages) / Start fresh" on relaunch.

---

## Recommendations (highest leverage first)
1. **Give gated Edit/Write a real inline approve button** (reuse the free-tier gate's card pattern) so Safe Auto / Approve-Edits can authorize an edit in-context instead of prose "Should I proceed?" → end. (F26)
2. **Raise or stream past the 965-byte command limit** (or chunk long edits) so legitimate multi-line commands/heredocs aren't rejected as "malformed". (F25)
3. **Add a convergence/step-budget guard** so a run that isn't producing diffs stops or checkpoints sooner; a $0.62 no-diff run should self-limit. (F27)
4. **Fix the free-tier confirmation to name the actually-selected provider** and label free models "not configured" when no key/quota, rather than a silent "no answer". (F29, F6/F7)
5. Tighten the "Completed" label for stopped/0-diff runs to "Stopped/Incomplete".

---

## ✅ RESOLVED — pass-2 fixes applied (same session)

| # | Fix | Files | Tests |
| --- | --- | --- | --- |
| **F26** | The account stream now collects the file paths whose Edit/Write the provider CLI refused; the pipeline returns `status: needs_edit_approval` with the exact files; the web UI renders an **"Allow edits once"** card that re-sends with `allowEditsOnce`, threaded down as `edit_grant` → the claude CLI's `--permission-mode acceptEdits` (Safe Auto only; Bash + destructive stay gated). **Also fixed a latent bug: the real Qt bridge dropped `allowCommand`, so even the F17 command-approve button was a no-op outside the mock** — now wired. | `opai/activity.py`, `opaihub/accounts.py`, `opai/app_state.py`, `opaihub/gui_pipeline.py`, `opai/gui_web.py`, `opai/assets/web/app.js`, `…/mock-bridge.js` | `tests/test_edit_approval.py`, `…/e2e/edit-approval.spec.js` |
| **F27** | No-progress guard in the account stream: an edit-intent run that reaches the step/time budget with **zero edit attempts** is checkpointed as `stuck_no_progress` (never green, honest partial). Env-tunable (`OPAI_NO_PROGRESS_STEP_BUDGET`=60, `OPAI_NO_PROGRESS_SECONDS`=600; 0 disables). | `opaihub/accounts.py`, `opai/app_state.py` | `tests/test_edit_approval.py::NoProgressGuardTests` |
| **F25** | Root cause is the **Claude Code CLI's own ~965-byte bash-parser limit** (external — OPai can't raise it). Primary trigger removed by F26 (the agent shelled out to giant PowerShell edits only because Safe Auto blocked the Edit tool). The cryptic "malformed syntax / too long for parsing" diagnostic is now rewritten to actionable guidance ("use the Edit/Write tools or split the command"). | `opai/activity.py` | `tests/test_edit_approval.py::CommandLengthErrorTests` |
| **F29** | The free-tier consent card now names the provider from the **result's** `free:<provider>:…` id, not a stale composer selection — Groq no longer shows "Send to Gemini". | `opai/assets/web/app.js` | `…/e2e/free-models.spec.js` (unchanged, still green) |
| **free "not configured"** | An empty free-tier response now names the provider and the concrete `*_API_KEY` to check, instead of a generic "did not return an answer". | `opaihub/gui_pipeline.py` | — |
| **"Completed" after stop** | Phase labels derive from canonical completion state: a stopped/stuck edit-intent run reads **"Stopped without finishing"**, not "Completed with no changes". | `opaihub/gui_pipeline.py` | covered by completion-contract suite |

Full suite after fixes: **Python 2232 passed / 3 skipped**, **Vitest 49**, **Playwright 41** (incl. 6 new approval specs). Lint clean.

*Chronological detail with verbatim quotes and screenshots: `scratchpad/OPAI_QA_RUN2.md`. Pass-1 detail: `scratchpad/OPAI_QA_RUN.md`.*
