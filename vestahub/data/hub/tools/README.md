# Hub Tools

This folder is for tool-specific adapters, install recipes, and overlays.

The central registry lives at `hub/registry/tools.yaml`. Do not add a tool adapter here until the registry entry exists.
