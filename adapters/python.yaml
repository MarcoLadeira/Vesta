detect:
  files: [pyproject.toml, requirements.txt, setup.py, pytest.ini]
commands:
  install: "python -m pip install -r requirements.txt"
  test: "pytest"
  targeted_test: "pytest {files}"
  lint: "ruff check ."
  format: "ruff format ."
